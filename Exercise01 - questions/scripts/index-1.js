// always use camel case stylization

// Declare
/** 1
 * Declare the variable called `high school`
 */
var highschool;



/** 2
 * Declare the variable called `oak tree`
 */
var oakTree;


/** 3
 * Declare the variable called `charging case`
 */
var chargingCase;



// Declare + Assign
/** 4
 * Declare the variable called `dyson`
 * assign to `Airwrap`
 */
var dyson = "Airwrap";




/** 5
 * Declare the variable called `google`
 * assign to `Nest Hub Max`
 */
var google = "Nest Hub Max";



/** 6
 * Declare the variable called `samsung`
 * assign to `Galaxy Note10+`
 */
var samsung = "Galaxy Note 10+";



// Initialise
/** 7
 * Initialise the variable `drivers license` to the value of 123456
 */
var driversLicense = 123456;



/** 8 
 * Initialise the variable `age` to the value of 50
 */
var age = 50;



/** 9
 * Initialise the variable `speed` to the value of 70
 */
var speed = 70;


/** 10
 * Initialise the variable `programming book` to the value of: Clean Code
 */
var programmingBook = "Clean Code";



/** 11
 * Initialise the variable `hotel` to the value of: 15C
 */
var hotel = "15C";




// Arithmetics
/** 12
 * Initialise the variable `container one` to the value of 5;
 * Initialise the variable `container two` to the value of 3;
 * Initialise the variable `total container` to the sum of container one and two
 * Console log out the variable total to make sure it shows in your console
 */
var containerOne = 5;
var containerTwo = 3;
var totalContainer = containerOne + containerTwo;
console.log(totalContainer);




/** 13
 * Initialise the variable `bag one` to the value of 20;
 * Initialise the variable `bag two` to the value of 10;
 * Initialise the variable `total bag` by subtracting bag two from bag one
 * Console log out the variable total bag to make sure it shows in your console
 */
var bagOne = 20;
var bagTwo = 10;
var totalBag = bagOne - bagTwo;
console.log(totalBag);



/** 14
 * Initialise the variable `day one` to the value of 5000;
 * Initialise the variable `day two` to the value of 10000;
 * Initialise the variable `day three` to the value of 12000;
 * Initialise the variable `average steps` by calculating the average based on the three days
 * Console log out the variable average steps and make sure it shows in your console
 */
var dayOne = 5000;
var dayTwo = 10000;
var dayThree = 12000;
var averageSteps = (dayOne + dayTwo + dayThree) / 3;


